// We want always to use the same type for the savd data representations, that's
// why we define aliases for common types
pub type PageId = u32;

pub type DatabaseKey = u32;

pub type PageSpace = u32;

pub type PageBuffer = Vec<u8>;